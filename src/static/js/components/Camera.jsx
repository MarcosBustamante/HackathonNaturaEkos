'use stict'

const React = require('react');

const Camera = React.createClass({
  render: function() {
    return (
      <div className="camera">
        Camera
      </div>
    );
  },
});

module.exports = Camera
