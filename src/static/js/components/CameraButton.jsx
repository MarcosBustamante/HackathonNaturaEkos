const React = require('react')

const CameraButton = React.createClass({
  render: function() {
    return (
      <div className="round-button">
        <div className="round-button-circle">
        </div>
      </div>
    );
  }
});

module.exports = CameraButton;
